version: 3

# 1) Install ngrok and add your authtoken (one-time):
#    ngrok config add-authtoken <YOUR_TOKEN>
# 2) Start the FastAPI server locally (default in this repo is port 9000)
# 3) Run: ngrok start --all --config ./ngrok.yml.example

tunnels:
  fastapi:
    proto: http
    addr: 9000
